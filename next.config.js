/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.wine.com.br', 'camo.githubusercontent.com', 'img.wine.com.br'],
  },
  reactStrictMode: true,
}

module.exports = nextConfig
